<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>07 miniproject</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-gray-900">
      <div class="form p-10">
        <form action="/create" method="POST">
          <input
            class="block w-full rounded-lg px-5 py-2 bg-zinc-800 text-white"
            type="text"
            placeholder="Title..." name="title"
          />
          <textarea
            class="block w-full rounded-lg px-5 py-2 bg-zinc-800 resize-none mt-2 text-white"
            placeholder="Write your task details..." name="details"
          ></textarea>
          <input
            class="bg-blue-600 mt-2 rounded-md px-5 py-2"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-5 flex-wrap">
        <% if(files.length > 0) { %>
          <% files.forEach((file) => { %>
          <div class="task w-72  px-3 py-4 rounded-lg bg-zinc-700">
          <h1 class="text-white text-2xl tracking-tighter"><%= file.title || file %></h1>
          <div class="flex w-full justify-between items-center mt-2">
            <a href="/files/<%= file.title || file %>" class="text-blue-500 inline-block ">Read More.</a>
          <a href="/edit/<%= file.title || file %>">✏️</a>
          </div>
        </div>
            <% }) %>
          <% } else{ %>
        <h3 class="text-white">No Tasks yet</h3>

          <% } %>
        <div class="task w-72 hidden px-3 py-4 rounded-lg bg-zinc-700">
          <h1 class="text-white text-2xl tracking-tighter">Some Tasks...</h1>
          <a href="" class="text-blue-500 inline-block mt-2">Read More.</a>
        </div>
        <div class="task w-72 hidden px-3 py-4 rounded-lg bg-zinc-700">
          <h1 class="text-white text-2xl tracking-tighter">Some Tasks...</h1>
          <a href="" class="text-blue-500 inline-block mt-2">Read More.</a>
        </div>

      </div>
    </div>
  </body>
</html>
